"""Static reference data (config, not code) — e.g. the Poya calendar.

Kept separate from agriforecast_ml's importable logic modules so a future
data file (public holidays, festival calendar, ...) has an obvious home.
"""
