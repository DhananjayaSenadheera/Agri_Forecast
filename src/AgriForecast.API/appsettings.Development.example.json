{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_comment_AllowedHosts": "Restrictive example for reference (F-13). In production set the real hostname. 'localhost' is a safe local-dev value; the tracked appsettings.json keeps '*' so plain local runs are not broken.",
  "AllowedHosts": "localhost",
  "Cors": {
    "_comment": "Local dev frontend origins only (F-07). Adjust to match your frontend dev-server port.",
    "AllowedOrigins": [
      "http://localhost:3000",
      "http://localhost:4173",
      "http://localhost:5173"
    ]
  },
  "Jwt": {
    "_comment": "Do NOT paste a real key here. The value below is a deliberate PLACEHOLDER that is under 32 bytes, so copying this file verbatim FAILS the startup guard (F-12) instead of shipping a known, guessable signing key. Generate your own strong key and supply it via .NET user-secrets: dotnet user-secrets set 'Jwt:Key' \"$(openssl rand -base64 48)\" — or the Jwt__Key env var in prod. Must be >= 32 bytes.",
    "Key": "<generate-your-own-key>"
  },
  "Auth": {
    "_comment": "API-9 admin bootstrap (documentation only — do NOT commit a value into appsettings.json). Set BootstrapAdminUsername to the username of an ALREADY-REGISTERED account; on the next startup, IF no admin exists yet, that user is promoted to Admin (once) and logged (username only). No admin credentials are ever stored in the repo — the account is created via POST /api/auth/register like any user, then promoted. Supply this via user-secrets (dotnet user-secrets set 'Auth:BootstrapAdminUsername' '<username>') or the Auth__BootstrapAdminUsername env var. Leave unset once an admin exists; a second run is a no-op.",
    "BootstrapAdminUsername": "<existing-username-to-promote-once>"
  }
}
